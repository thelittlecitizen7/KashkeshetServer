using KashkeshtWorkerServiceServer.Src.Enums;
using KashkeshtWorkerServiceServer.Src.Models.ChatModel;
using System;
using System.Collections.Generic;
using System.Text;

namespace KashkeshtWorkerServiceServer.Src.Factory
{
    public class ChatCreatorFactory
    {
       /* public static ChatModel Create(ChatType chatType) 
        {
            if (chatType == ChatType.Private)
            {
                return new GlobalChat();
            }
            else if (chatType == ChatType.Private)
            {
                return new PrivateChat();
            }
            else if (chatType == ChatType.Group)
            {
                return new GroupChat();
            }
            else 
            {
                return null;
            }
            
        }*/
    }
}
